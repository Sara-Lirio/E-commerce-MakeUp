import React from 'react'
import styled from 'styled-components'

const StyledMenu = styled.section`
border: 1px solid var(--cinza-color);
border-radius: 20px;
width: 17em;
max-height: 25em;
box-shadow: 0 0 .8em var(--gelo-color);

`

const MenuProoducts = () => {
  return (
    <StyledMenu>Menu</StyledMenu>
  )
}

export default MenuProoducts